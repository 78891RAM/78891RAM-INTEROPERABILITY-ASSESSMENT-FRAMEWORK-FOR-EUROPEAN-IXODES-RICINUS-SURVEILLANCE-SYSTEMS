dash==4.4.1
dash-leaflet==1.1.3
numpy==2.5.1
pandas==3.0.3
plotly==5.24.1
openpyxl==3.1.5
gunicorn==26.0.0
pytest==8.3.4
scikit-learn==1.6.1
joblib==1.4.2
shapely==2.1.2
kaleido==0.2.1

# --- Dev-only, NOT needed by the deployed app ---
# The live dashboard only reads pre-generated GeoJSON boundary files and
# pre-rendered raster-overlay PNGs (see ui/maps/) — geopandas/rasterio are
# only imported by scripts/generate_raster_overlays.py, run locally
# whenever a source raster changes, never at Render runtime. Install with:
#   venv/bin/python -m pip install geopandas rasterio matplotlib pillow
# geopandas==1.1.4
# rasterio==1.5.1
# matplotlib
# pillow
