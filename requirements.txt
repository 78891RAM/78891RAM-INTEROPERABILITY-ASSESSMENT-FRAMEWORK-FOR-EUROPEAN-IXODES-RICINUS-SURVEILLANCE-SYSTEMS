dash==4.4.1
dash-leaflet==1.1.3
numpy==2.5.1
pandas==3.0.3
plotly==5.24.1
openpyxl==3.1.5
gunicorn==26.0.0
pytest==8.3.4
scikit-learn==1.6.1
joblib==1.4.2
shapely==2.1.2
kaleido==0.2.1

# --- Dev-only, NOT needed by the deployed app ---
# The live dashboard only reads pre-generated GeoJSON boundary files and
# pre-rendered raster-overlay PNGs (see ui/maps/) — geopandas/rasterio are
# only imported by scripts/generate_raster_overlays.py, run locally
# whenever a source raster changes, never at Render runtime. Install with:
#   venv/bin/python -m pip install geopandas rasterio matplotlib pillow
# geopandas==1.1.4
# rasterio==1.5.1
# matplotlib
# pillow
#
# playwright is only imported by scripts/export_leaflet_figures.py, which
# screenshots the two dash-leaflet maps for dissertation figures — the
# leaflet maps render client-side (real browser, OSM tiles, JS-styled
# GeoJSON), so unlike the Plotly figures there's no Python object Kaleido
# can rasterize; this drives an actual headless browser instead. Install:
#   venv/bin/python -m pip install playwright
#   venv/bin/python -m playwright install chromium
# playwright==1.62.0
