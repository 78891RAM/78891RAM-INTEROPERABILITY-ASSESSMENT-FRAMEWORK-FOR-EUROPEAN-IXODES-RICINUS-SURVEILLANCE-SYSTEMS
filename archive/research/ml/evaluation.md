# Evaluation

The held-out test set reports accuracy, precision, recall, F1 and ROC-AUC, alongside a confusion matrix, ROC curve and JSON classification report. Cross-validation mean and standard deviation are included in `metrics.json`.
